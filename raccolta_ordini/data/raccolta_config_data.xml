<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ================================= -->
        <!-- CONFIGURAZIONE DEFAULT             -->
        <!-- ================================= -->

        <!-- Configurazione default viene creata dal post_init_hook -->
        <!-- Questo file è pronto per eventuali configurazioni aggiuntive -->

        <!-- ================================= -->
        <!-- DATI DEMO (OPZIONALI)             -->
        <!-- ================================= -->

        <!-- Configurazione Demo (solo se necessaria) 
        <record id="raccolta_config_demo" model="raccolta.config">
            <field name="name">Configurazione Demo</field>
            <field name="active" eval="False"/>
            <field name="warehouse_id" ref="stock.warehouse0"/>
            <field name="receipt_format">48mm</field>
            <field name="auto_create_ddt" eval="True"/>
            <field name="use_agent_numbering" eval="True"/>
        </record>
        -->

    </data>
</odoo>